services:
  - type: web
    name: odoo-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 odoo-bin -c odoo.conf -d odoo_db_tdy9 -i base --without-demo=all --stop-after-init
    envVars:
      - key: PYTHON_VERSION
        value: 3.10